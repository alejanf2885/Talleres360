namespace Talleres360.Enums.Auth
{
    public enum AuthErrorCode
    {
        CREDENCIALES_INCORRECTAS,
        CUENTA_INACTIVA,
        CUENTA_BLOQUEADA,
        EMAIL_NO_VERIFICADO,
        TOKEN_INVALIDO,
        REGISTRO_FALLIDO,
        REFRESH_TOKEN_INVALIDO,
        REFRESH_TOKEN_EXPIRADO,
        DATOS_INVALIDOS,
        ERROR_GENERICO
    }
}
